<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a22housexam2">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:name=".App"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoadingActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activities.MainActivity"
           >

        </activity>
        <activity android:name=".Activities.MainViewActivity">

        </activity>
        <activity android:name=".Activities.PcInfoActivity">

        </activity>
<!--        <activity android:name=".OneFrag"></activity>-->
<!--        <activity android:name=".TwoFrag"></activity>-->
<!--        <activity android:name=".ThreeFrag"></activity>-->
        <activity android:name=".Activities.RoomInfoActivity" ></activity>
    </application>

</manifest>